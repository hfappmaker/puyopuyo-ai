[package]
name = "game-ai"
version = "0.1.0"
edition = "2021"

[features]
default = []
nn = ["burn"]

[dependencies]
burn = { version = "0.16", default-features = false, features = ["ndarray"], optional = true }
serde = { version = "1", features = ["derive"] }
bincode = "1"
